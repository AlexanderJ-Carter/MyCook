# MyCook Docker Compose
#
# 轻量版（默认）:  docker compose up -d --build
# 完整版（图片）:    docker compose --profile full up -d --build
# 开发:             docker compose --profile dev up

services:
  mycook:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        COOKLIKEHOC_BRANCH: main
        HOWTOCOOK_BRANCH: master
        SKIP_IMAGES: "1"
    image: mycook:lite
    container_name: mycook
    ports:
      - "${Mycook_PORT:-8080}:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:80/"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s

  mycook-full:
    profiles: [full]
    build:
      context: .
      dockerfile: Dockerfile
      args:
        COOKLIKEHOC_BRANCH: main
        HOWTOCOOK_BRANCH: master
        SKIP_IMAGES: "0"
    image: mycook:full
    container_name: mycook-full
    ports:
      - "${Mycook_PORT:-8080}:80"
    restart: unless-stopped

  mycook-dev:
    profiles: [dev]
    image: node:22-alpine
    container_name: mycook-dev
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    environment:
      SYNC_PULL: "0"
    command: sh -c "npm install && npm run sync && npm run docs:dev -- --host 0.0.0.0"

  mycook-mcp:
    profiles: [agent]
    build:
      context: .
      dockerfile: Dockerfile.mcp
    image: mycook-mcp:latest
    container_name: mycook-mcp
    ports:
      - "${MCP_PORT:-3001}:3001"
    environment:
      SITE_URL: ${SITE_URL:-http://localhost:8080}
    restart: unless-stopped
