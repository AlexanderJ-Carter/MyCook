{
  "name": "my-cook",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-upstream.js",
    "docs:dev": "node scripts/generate-recent.js && vitepress dev .",
    "docs:build": "node scripts/generate-recent.js && vitepress build .",
    "docs:preview": "vitepress preview .",
    "build": "npm run docs:build",
    "start": "npm run docs:dev"
  },
  "devDependencies": {
    "vitepress": "^1.3.4"
  }
}